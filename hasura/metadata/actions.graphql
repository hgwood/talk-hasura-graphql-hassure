type Query {
  render_pdf(
    annee: Int!
    mois: Int!
  ): File
}

type Query {
  render_pdf_async(
    annee: Int!
    mois: Int!
  ): File
}

type File {
  id: uuid!
  name: String!
  content: String!
}

